{
  "planPath": "docs/superpowers/plans/2026-06-07-layer-5a-luma-retirement.md",
  "tasks": [
    {
      "id": 8,
      "subject": "Task 1: Source-tree deletion + edits + label sweep",
      "status": "completed",
      "commitSha": "20ad7d9",
      "description": "**Goal:** Remove every code path that calls the retired Luma direct API and every test that imports `LumaEngine`/`LumaBackend`; sweep the three peripheral test files that use `provider=\"luma\"` as a free-form `LocalOutputSink` label. Single atomic commit.\n\n**Files:**\n- Delete: `src/kinoforge/engines/luma/__init__.py`\n- Delete: `tests/engines/test_luma.py`\n- Delete: `examples/configs/comparison/luma-t2v.yaml`\n- Modify: `src/kinoforge/_adapters.py` (drop `engines.luma` import)\n- Modify: `src/kinoforge/core/config.py` (drop `\"luma\"` from `KNOWN_ENGINES`)\n- Modify: `tests/test_core_invariant.py` (drop `lumaai` confinement tuple)\n- Modify: `tests/test_examples.py` (tighten kind set to `{\"replicate\",\"runway\"}`)\n- Modify: `tests/pipeline/test_generate_clip.py`, `tests/outputs/test_local.py`, `tests/outputs/test_format_filename.py` (label sweep + cascading assertions)\n\n**Acceptance Criteria:**\n- engines/luma/, test_luma.py, comparison/luma-t2v.yaml gone\n- 0 hits for `LumaEngine|LumaBackend|engines\\.luma|engines/luma|provider=\"luma\"` in src/tests/examples\n- `import kinoforge._adapters` exits 0\n- lint/format/typecheck/test/pre-commit all green\n- test count exactly 13 lower than baseline\n- working tree clean after commit\n\n**Verify:** `pixi run lint && pixi run format && pixi run typecheck && pixi run test && pixi run pre-commit run --all-files`\n\n```json:metadata\n{\"files\": [\"src/kinoforge/engines/luma/__init__.py\", \"tests/engines/test_luma.py\", \"examples/configs/comparison/luma-t2v.yaml\", \"src/kinoforge/_adapters.py\", \"src/kinoforge/core/config.py\", \"tests/test_core_invariant.py\", \"tests/test_examples.py\", \"tests/pipeline/test_generate_clip.py\", \"tests/outputs/test_local.py\", \"tests/outputs/test_format_filename.py\"], \"verifyCommand\": \"pixi run lint && pixi run format && pixi run typecheck && pixi run test && pixi run pre-commit run --all-files\", \"acceptanceCriteria\": [\"engines/luma gone\", \"test_luma.py gone\", \"luma-t2v.yaml gone\", \"orphan-grep returns 0\", \"adapter sanity import 0\", \"gate all green\", \"count drops by exactly 13\", \"tree clean\"]}\n```"
    },
    {
      "id": 9,
      "subject": "Task 2: README tombstone + PROGRESS Phase 44 entry + pyproject orphan cleanup",
      "status": "pending",
      "blockedBy": [8],
      "description": "**Goal:** Replace README's Hosted-Bearer Luma row with a forward-pointing tombstone, strip 'Luma' from surrounding headings + prose, record Layer 5a in PROGRESS under a new Phase 44 block, AND drop the now-orphaned `\"lumaai.*\"` entry from the mypy override list in pyproject.toml (post-Task-1 cleanup folded into the housekeeping commit per both reviewers' guidance). Atomic commit; depends on Task 1.\n\n**Files:**\n- Modify: `README.md` (4 sites — bearer-strategy row, Hosted Bearer heading, table row, echo line)\n- Modify: `PROGRESS.md` (Phase 44 block; Phase 43 Task 10 count fix; Phase 43 Task 13 DEFERRED→CLOSED; RESUME pointer sweep)\n- Modify: `pyproject.toml` (drop `\"lumaai.*\"` from the `[[tool.mypy.overrides]]` module list at line 62; mypy emits `note: unused section(s): module = ['lumaai.*']` until this is removed)\n\n**Acceptance Criteria:**\n- README section heading reads `## Hosted Bearer providers (Replicate / Runway)`\n- README auth-strategy table row reads `(fal, Replicate, Runway)`\n- README Bearer wire-shape table loses the Luma row\n- Tombstone paragraph below the table points to Bedrock + Replicate + Layer 5b\n- `LUMAAI_API_KEY` echo line recommented for Layer 5b reservation\n- PROGRESS Phase 44 block appended at the end of the Post-MVP section\n- Phase 43 Task 10 `3 of 15` → `2 of 15` with luma-t2v.yaml removal annotation\n- Phase 43 Task 13 DEFERRED → CLOSED with retirement note\n- RESUME pointer swept (no-op if no Luma direct-API mention)\n- `pyproject.toml` mypy overrides line no longer contains `\"lumaai.*\"`; `pixi run typecheck` no longer emits the `unused section(s)` note\n- `pixi run pre-commit run --all-files` green\n\n**Verify:** `pixi run pre-commit run --all-files` exits 0; sweep grep returns only allowed hits.\n\n```json:metadata\n{\"files\": [\"README.md\", \"PROGRESS.md\", \"pyproject.toml\"], \"verifyCommand\": \"pixi run pre-commit run --all-files\", \"acceptanceCriteria\": [\"README heading drops Luma\", \"bearer-strategy row drops Luma\", \"Bearer table loses Luma row\", \"tombstone present\", \"LUMAAI_API_KEY echo recommented\", \"Phase 44 block appended\", \"Phase 43 Task 10 count fixed to 2 of 15\", \"Phase 43 Task 13 flipped to CLOSED\", \"RESUME pointer swept\", \"pyproject.toml orphan lumaai.* mypy override removed\", \"pre-commit green\"]}\n```"
    }
  ],
  "lastUpdated": "2026-06-07T20:30:00-07:00"
}
