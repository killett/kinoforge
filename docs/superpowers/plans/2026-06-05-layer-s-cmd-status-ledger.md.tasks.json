{
  "planPath": "docs/superpowers/plans/2026-06-05-layer-s-cmd-status-ledger.md",
  "tasks": [
    {
      "id": 7,
      "subject": "Task 1: Extend Ledger.record schema with idle_timeout_s + max_age_s",
      "status": "completed",
      "description": "**Goal:** Persist optional `idle_timeout_s` and `max_age_s` keys on every new ledger entry so the operator's `kinoforge status` can surface lifecycle policy without re-loading the YAML config.\n\n**Files:**\n- Modify: `src/kinoforge/core/lifecycle.py:443-467` — `Ledger.record` signature + entry dict\n- Modify: `src/kinoforge/cli.py:305-307` — `_cmd_deploy` thread `cfg.lifecycle()` values into the call\n- Test: `tests/core/test_lifecycle.py` — three new tests (legacy entry read, new-kwargs round-trip, omit-on-None round-trip)\n\n**Acceptance Criteria:**\n- Ledger.record persists idle_timeout_s + max_age_s when supplied\n- Ledger.record omits the keys when kwargs are None (backwards-compat)\n- Ledger.entries reads legacy entries without raising\n- _cmd_deploy threads cfg.lifecycle() values into ledger.record\n- existing test_lifecycle_sweeper + test_cli call sites pass unchanged\n\n**Verify:** `pixi run pytest tests/core/test_lifecycle.py tests/core/test_lifecycle_sweeper.py tests/test_cli.py -v`\n\n```json:metadata\n{\"files\": [\"src/kinoforge/core/lifecycle.py\", \"src/kinoforge/cli.py\", \"tests/core/test_lifecycle.py\", \"tests/test_cli.py\"], \"verifyCommand\": \"pixi run pytest tests/core/test_lifecycle.py tests/core/test_lifecycle_sweeper.py tests/test_cli.py -v\", \"acceptanceCriteria\": [\"Ledger.record persists idle_timeout_s + max_age_s when supplied\", \"Ledger.record omits the keys when kwargs are None (backwards-compat)\", \"Ledger.entries reads legacy entries without raising\", \"_cmd_deploy threads cfg.lifecycle() values into ledger.record\", \"existing test_lifecycle_sweeper + test_cli call sites pass unchanged\"]}\n```"
    },
    {
      "id": 8,
      "subject": "Task 2: Rewrite _cmd_status for ledger-first dispatch + rich key=value output",
      "status": "pending",
      "blockedBy": [7],
      "description": "**Goal:** Replace the current local-provider-only `_cmd_status` with a ledger-first implementation that prints an alphabetised `key=value` block of ledger-derived facts, dispatches to the recorded provider for live status / endpoints, and distinguishes stale-ledger (exit 0 + advisory) from transient provider failure (exit 2).\n\n**Files:**\n- Modify: `src/kinoforge/cli.py:217-219` — `status` subparser (add `--config`/`-c` optional flag)\n- Modify: `src/kinoforge/cli.py:550-579` — `_cmd_status` body (full rewrite)\n- Create: `src/kinoforge/cli.py` (new helpers) — `_build_ledger_block`, `_ledger_field_or_cfg`, `_print_status_block`\n- Test: `tests/test_cli.py` — sixteen new tests covering the spec's test matrix items #4-#17\n\n**Acceptance Criteria:**\n- _build_ledger_block returns spec-defined fields in alphabetical order with documented fallback precedence\n- negative age_h is clamped to 0.0\n- created_at uses local-timezone ISO8601 with colon-separated offset\n- _cmd_status ledger-missing path returns 1 + stderr message\n- provider success path prints provider_status + endpoints + exit 0\n- KeyError path prints advisory exactly once + exit 0\n- UnknownAdapter path prints unknown-provider line + exit 2\n- non-KeyError exceptions print unknown line w/ ExcClass + exit 2\n- endpoints() failure prints endpoints=unknown(...) + exit stays 0\n- status --id ID -c PATH parses + cfg fills missing legacy fields\n\n**Verify:** `pixi run pytest tests/test_cli.py -v -k 'status or build_ledger_block or print_status_block'`\n\n```json:metadata\n{\"files\": [\"src/kinoforge/cli.py\", \"tests/test_cli.py\"], \"verifyCommand\": \"pixi run pytest tests/test_cli.py -v -k 'status or build_ledger_block or print_status_block'\", \"acceptanceCriteria\": [\"_build_ledger_block returns spec-defined fields in alphabetical order with documented fallback precedence\", \"negative age_h is clamped to 0.0\", \"created_at uses local-timezone ISO8601 with colon-separated offset\", \"_cmd_status ledger-missing path returns 1 + stderr message\", \"provider success path prints provider_status + endpoints + exit 0\", \"KeyError path prints advisory exactly once + exit 0\", \"UnknownAdapter path prints unknown-provider line + exit 2\", \"non-KeyError exceptions print unknown line w/ ExcClass + exit 2\", \"endpoints() failure prints endpoints=unknown(...) + exit stays 0\", \"status --id ID -c PATH parses + cfg fills missing legacy fields\"]}\n```"
    },
    {
      "id": 9,
      "subject": "Task 3: Ship kinoforge forget --id <id> recovery subcommand + README + PROGRESS",
      "status": "pending",
      "blockedBy": [8],
      "description": "**Goal:** Add a single-id ledger-only `forget` subcommand so operators can clear the stale entries that `_cmd_status` advises them about. Document `status` and `forget` in README. Update PROGRESS Phase 33.\n\n**Files:**\n- Modify: `src/kinoforge/cli.py` — `_build_parser` (add `forget` subcommand), `main` dispatch (add `cmd == \"forget\"` branch), `_cmd_forget` impl\n- Modify: `tests/test_cli.py` — four new tests covering #18-#21 from the spec test matrix\n- Modify: `README.md` — \"Operator commands\" section gains `status` / `forget` example\n- Modify: `PROGRESS.md` — append \"Phase 33 — Layer S\" entry; flip PROGRESS:120 carry-forward to CLOSED\n\n**Acceptance Criteria:**\n- forget --id ID removes existing entry, prints 'forgot: <id>', exit 0\n- forget --id <missing> emits stderr 'not found in ledger', exit 1\n- second forget on same id returns exit 1 (non-idempotent regression lock)\n- _build_parser registers forget subcommand with --id flag\n- README documents kinoforge status + forget operator workflow\n- PROGRESS Phase 33 entry committed with T1/T2/T3 SHAs + PROGRESS:120 flipped to CLOSED\n\n**Verify:** `pixi run pytest tests/test_cli.py -v -k 'forget' && pixi run pytest -x`\n\n```json:metadata\n{\"files\": [\"src/kinoforge/cli.py\", \"tests/test_cli.py\", \"README.md\", \"PROGRESS.md\"], \"verifyCommand\": \"pixi run pytest tests/test_cli.py -v -k 'forget' && pixi run pytest -x\", \"acceptanceCriteria\": [\"forget --id ID removes existing entry, prints 'forgot: <id>', exit 0\", \"forget --id <missing> emits stderr 'not found in ledger', exit 1\", \"second forget on same id returns exit 1 (non-idempotent regression lock)\", \"_build_parser registers forget subcommand with --id flag\", \"README documents kinoforge status + forget operator workflow\", \"PROGRESS Phase 33 entry committed with T1/T2/T3 SHAs + PROGRESS:120 flipped to CLOSED\"]}\n```"
    }
  ],
  "lastUpdated": "2026-06-05T01:00:00-07:00"
}
