{
  "planPath": "docs/superpowers/plans/2026-09-23-u53-needs-only-embed.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Embed-closure guard test",
      "status": "completed",
      "description": "**Goal:** A test that computes each RunPod diffusers config's servers/ import closure from its rendered provision script and asserts the embedded set equals it, failing RED today on the three unimported modules.\n\n**Files:**\n- Create: `tests/providers/test_pod_embed_closure.py`\n\n**Acceptance Criteria:**\n- [ ] The closure helper follows nested (function-level) imports and skips `if TYPE_CHECKING:` blocks\n- [ ] Entry points are discovered from the rendered provision script's `python -m kinoforge…` lines, not hardcoded\n- [ ] `test_every_imported_servers_module_is_embedded` passes on all thirteen configs today\n- [ ] `test_no_unimported_servers_module_is_embedded` FAILS on all thirteen configs today, naming `_av_io`, `_lora` and `minimax_h3_server`\n- [ ] The module docstring records that the static closure is sound only because no embedded module dynamically imports a `kinoforge.*` module, and names the three `importlib.import_module` sites that were checked\n\n**Verify:** `pixi run pytest tests/providers/test_pod_embed_closure.py -v` -> `test_every_imported…` 13 passed, `test_no_unimported…` 13 failed naming the three modules\n\n```json:metadata\n{\"files\": [\"tests/providers/test_pod_embed_closure.py\"], \"verifyCommand\": \"pixi run pytest tests/providers/test_pod_embed_closure.py -v\", \"acceptanceCriteria\": [\"closure follows nested imports and skips TYPE_CHECKING\", \"entry points discovered from the rendered script, not hardcoded\", \"under-embed direction passes on all 13 configs\", \"over-embed direction fails on all 13, naming the three H3-only modules\", \"docstring names the dynamic-import soundness assumption and the 3 checked sites\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: Live RED probe — oversized create",
      "status": "completed",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** Record, before any config changes, whether `runpod-diffusers-spandrel-x2-upscale` (112,831 B) is refused by `podFindAndDeployOnDemand` today — the red half of the live proof, at $0.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `tests/live/evidence/2026-09-23-u53-env-payload/red-oversized-create.txt`\n\n**Acceptance Criteria:**\n- [ ] `pixi run preflight` exits 0 before the run\n- [ ] The probe uses the shipped `kinoforge upscale` path — no bespoke tool, so no hand-written GraphQL can drift from the real mutation\n- [ ] The literal failure (or success) is captured verbatim to the evidence file and committed\n- [ ] If the create SUCCEEDS, `--no-reuse` auto-destroys the pod, `kinoforge list` confirms it AFTER the orchestrator exits, and the result is reported as \"the ~101 KB figure is looser than recorded\" rather than dropped\n- [ ] The rendered env byte count is recorded alongside the outcome\n\n**Verify:** `cat tests/live/evidence/2026-09-23-u53-env-payload/red-oversized-create.txt` -> shows the measured byte count and either a create failure, or a created pod plus confirmed teardown\n\n```json:metadata\n{\"userGate\": true, \"tags\": [\"user-gate\"], \"requireEvidenceTokens\": [[\"112,831\", \"rendered env\"], [\"No running instances\", \"No instances recorded in ledger\"]], \"files\": [\"tests/live/evidence/2026-09-23-u53-env-payload/red-oversized-create.txt\"], \"verifyCommand\": \"cat tests/live/evidence/2026-09-23-u53-env-payload/red-oversized-create.txt\", \"acceptanceCriteria\": [\"preflight exits 0 and tree is clean before the run\", \"probe uses the shipped kinoforge upscale path, no bespoke tool\", \"literal create outcome captured verbatim to the evidence file\", \"kinoforge list AFTER the orchestrator exits shows both no-instances lines\", \"rendered env byte count recorded beside the outcome\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 3: Switch 13 configs to needs-only embeds",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Replace each RunPod diffusers config's `kinoforge.engines.diffusers.servers` package embed with `embed_files` entries for the three modules its pod imports, turning Task 1's guard green.\n\n**Files:**\n- Modify: the 13 `examples/configs/runpod-diffusers-*.yaml` pod configs (exact per-file line numbers in the plan's Task 3 Files section)\n\n**Acceptance Criteria:**\n- [ ] Every config's `embed_modules` list no longer contains `\"kinoforge.engines.diffusers.servers\"`; the key is removed entirely where that was its only entry\n- [ ] Every config's `embed_files` contains the three server modules, merged into any existing `embed_files` list rather than duplicating the key\n- [ ] `kinoforge.upscalers.flashvsr`, `kinoforge.interpolators.rife` and `kinoforge.upscalers.spandrel` `embed_modules` entries are unchanged where present\n- [ ] Both directions of Task 1's guard pass on all thirteen configs (26 passed)\n- [ ] Every config still loads (`load_config` raises nothing)\n- [ ] Measured rendered env for `runpod-diffusers-wan-2_2-14b-t2v-flashvsr-1080p-upscale` is 88,575 B +/- 200 B\n\n**Verify:** `pixi run pytest tests/providers/test_pod_embed_closure.py tests/test_examples.py -v` -> all passed\n\n```json:metadata\n{\"files\": [\"examples/configs/runpod-diffusers-*.yaml\"], \"verifyCommand\": \"pixi run pytest tests/providers/test_pod_embed_closure.py tests/test_examples.py -v\", \"acceptanceCriteria\": [\"servers package embed removed from all 13 configs\", \"three server modules added to each config's embed_files\", \"upscaler/interpolator embed_modules entries untouched\", \"guard passes both directions, 26 passed\", \"all configs still load\", \"worst config measures 88,575 B +/- 200 B\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Re-baseline ratchet, retire exemption",
      "status": "completed",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** Lower `_BASELINE_BYTES` to the new measurements and replace the dated `_SAFE_AS_OF_2026_09_22` name list with an unconditional \"every RunPod diffusers config is under the ceiling\" assertion.\n\n**Files:**\n- Modify: `tests/providers/test_env_payload_ceiling.py` (module docstring, `_BASELINE_BYTES`, `_SAFE_AS_OF_2026_09_22`, the ceiling test)\n\n**Acceptance Criteria:**\n- [ ] `_BASELINE_BYTES` holds the thirteen new measurements from Task 3 Step 5, every value under 101,000\n- [ ] `_SAFE_AS_OF_2026_09_22` is deleted; `rg -n '_SAFE_AS_OF' tests/` returns nothing\n- [ ] `test_configs_safe_as_of_2026_09_22_never_cross_the_ceiling` is replaced by a test parametrised over every RunPod diffusers pod config asserting `measured < _RUNPOD_CEILING_BYTES`\n- [ ] The module docstring's \"ALREADY OVER the ~101 KB ceiling\" section and its eight-name list are replaced by a record of the U53 fix\n- [ ] The re-baselining instructions at the end of the docstring still work verbatim\n\n**Verify:** `pixi run pytest tests/providers/test_env_payload_ceiling.py -v` -> all passed, 13 ceiling tests\n\n```json:metadata\n{\"files\": [\"tests/providers/test_env_payload_ceiling.py\"], \"verifyCommand\": \"pixi run pytest tests/providers/test_env_payload_ceiling.py -v\", \"acceptanceCriteria\": [\"_BASELINE_BYTES holds the 13 new measurements, all under 101,000\", \"_SAFE_AS_OF_2026_09_22 deleted, no references remain\", \"ceiling test is unconditional over every RunPod diffusers config\", \"docstring breach section replaced by the U53 fix record\", \"re-baselining instructions still work verbatim\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 5: Re-snapshot launch-payload goldens",
      "status": "completed",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** Regenerate the affected launch-payload goldens so the golden tests reflect the new embed set, and confirm nothing outside the 13 RunPod diffusers configs moved.\n\n**Files:**\n- Modify: `tests/providers/golden/launch_payloads/runpod-diffusers-*.json` (13 files)\n\n**Acceptance Criteria:**\n- [ ] `pixi run pre-commit run --all-files` is run and green BEFORE the snapshot tool (regenerating first produces goldens the formatter invalidates)\n- [ ] Exactly the 13 RunPod diffusers goldens change; the other 27 are byte-identical\n- [ ] `tests/engines/diffusers/_golden_provision.json` is unchanged — its two configs are Modal, out of scope (verified, not assumed)\n- [ ] `pixi run pytest tests/providers/test_launch_payload_goldens.py tests/engines/diffusers/test_render_provision_split.py -v` passes\n- [ ] `pixi run test -q` reports 0 failed\n\n**Verify:** `pixi run pytest tests/providers/test_launch_payload_goldens.py tests/engines/diffusers/test_render_provision_split.py -v` -> all passed\n\n```json:metadata\n{\"files\": [\"tests/providers/golden/launch_payloads/\"], \"verifyCommand\": \"pixi run pytest tests/providers/test_launch_payload_goldens.py tests/engines/diffusers/test_render_provision_split.py -v\", \"acceptanceCriteria\": [\"pre-commit --all-files green BEFORE the snapshot tool\", \"exactly 13 runpod-diffusers goldens changed, other 27 byte-identical\", \"_golden_provision.json unchanged\", \"golden tests pass\", \"full suite 0 failed\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: Correct three stale doc claims",
      "status": "completed",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** Fix the \"~4x headroom\" claim in CLAUDE.md, its duplicate as a source comment, and U53's inverted fix direction in PROGRESS.md.\n\n**Files:**\n- Modify: `CLAUDE.md:372-386` (Known infra gotchas -> the RunPod 500 subsection)\n- Modify: `src/kinoforge/providers/runpod/__init__.py:1316-1322` (comment in `_encode_provision_script`)\n- Modify: `PROGRESS.md:694` (U53 row), `PROGRESS.md:3356-3368` (suggested order), `PROGRESS.md:473` (provenance), `PROGRESS.md:594,607` (counts)\n\n**Acceptance Criteria:**\n- [ ] CLAUDE.md states the measured behaviour: the outer gzip saves ~26% (44,332 B on the worst config), it must NOT be dropped, and the real lever is what gets embedded\n- [ ] The `_encode_provision_script` comment no longer claims ~4x headroom and points at the closure guard\n- [ ] U53's PROGRESS.md row is marked FIXED, and its \"drop the outer gzip\" fix direction is corrected as measured backwards\n- [ ] The suggested-order entry at `PROGRESS.md:3360` no longer tells the next session to drop the outer gzip\n- [ ] STATUS INDEX counts updated: forty-five -> forty-six fixed, ten -> nine open, U53 moved between the lists, provenance sentence inserted\n- [ ] No other U-item row is altered\n\n**Verify:** `rg -n '4× headroom|4x headroom|near-zero payoff' CLAUDE.md src/ PROGRESS.md` -> matches only inside the correction notes that quote the old claim to refute it\n\n```json:metadata\n{\"files\": [\"CLAUDE.md\", \"src/kinoforge/providers/runpod/__init__.py\", \"PROGRESS.md\"], \"verifyCommand\": \"rg -n '4\\u00d7 headroom|4x headroom|near-zero payoff' CLAUDE.md src/ PROGRESS.md\", \"acceptanceCriteria\": [\"CLAUDE.md records the measured 26.2% saving and says the gzip stays\", \"source comment corrected and points at the closure guard\", \"U53 row marked FIXED with the backwards-direction correction\", \"suggested-order entry no longer says to drop the outer gzip\", \"STATUS INDEX counts and provenance updated\", \"no other U-item row altered\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 7: Live GREEN proof — create and serve",
      "status": "completed",
      "blockedBy": [
        5
      ],
      "description": "**Goal:** Prove on real RunPod hardware that a previously-over-ceiling config now creates successfully AND that the pod boots and serves without the three dropped modules.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `tests/live/evidence/2026-09-23-u53-env-payload/green-create-and-upscale.txt`\n- Modify: `successful-generations.md` (See-also line only — existing (runpod, diffusers, spandrel, upscale) tuple)\n\n**Acceptance Criteria:**\n- [ ] `pixi run preflight` exits 0 and the tree is clean before any spend\n- [ ] `runpod-diffusers-spandrel-x2-upscale` — the same config Task 2 probed — creates a pod successfully (no HTTP 500)\n- [ ] The pod reaches ready and serves an `/upscale` against `examples/configs/grids/_fixtures/wan21_strength_cell0.mp4`, producing a playable output video\n- [ ] `/util` was polled every 60-90 s and `gpuUtilPercent` read non-zero during the upscale; `est_spend` was NOT used as the health signal\n- [ ] Frames extracted with `kinoforge.core.frames.ffmpeg_frames_by_count` and visually reviewed; verdict recorded in the evidence file\n- [ ] `--no-reuse` was passed, and `pixi run kinoforge list` AFTER the orchestrator exited shows BOTH no-instances lines\n- [ ] The evidence file cites Task 2's red result explicitly\n- [ ] Total spend recorded and under $1.00\n\n**Verify:** `cat tests/live/evidence/2026-09-23-u53-env-payload/green-create-and-upscale.txt` -> shows create success, non-zero GPU util, frame-QA verdict, and the post-run `kinoforge list` output\n\n```json:metadata\n{\"userGate\": true, \"tags\": [\"user-gate\"], \"requireEvidenceTokens\": [[\"RED\", \"red-oversized-create\", \"112,831\"], [\"GREEN\", \"pod created\", \"73,369\"]], \"files\": [\"tests/live/evidence/2026-09-23-u53-env-payload/green-create-and-upscale.txt\", \"successful-generations.md\"], \"verifyCommand\": \"cat tests/live/evidence/2026-09-23-u53-env-payload/green-create-and-upscale.txt\", \"acceptanceCriteria\": [\"preflight exits 0 and tree is clean before spend\", \"the previously-over-ceiling config creates a pod with no HTTP 500\", \"pod serves /upscale and produces a playable output video\", \"gpuUtilPercent polled every 60-90s and read non-zero during the upscale\", \"frames extracted and visually reviewed, verdict recorded\", \"--no-reuse passed and kinoforge list AFTER exit shows both no-instances lines\", \"evidence file cites Task 2's red result\", \"spend recorded and under $1.00\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-09-23T18:30:09"
}
