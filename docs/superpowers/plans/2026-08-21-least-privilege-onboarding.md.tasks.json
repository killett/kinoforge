{
  "planPath": "docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md",
  "specPath": "docs/superpowers/specs/2026-08-21-least-privilege-onboarding-design.md",
  "tasks": [
    {
      "id": 0,
      "nativeTaskId": "1",
      "subject": "Task 0: Identifier scanner module",
      "status": "completed",
      "description": "**Goal:** A reusable scanner that finds concrete cloud identifiers in text, with a line pragma escape hatch, mirroring the shape of `tools/scan_secrets.py`.\n\n**Files:**\n- Create: `tools/scan_identifiers.py`\n- Test: `tests/tools/test_scan_identifiers.py`\n\n**Acceptance Criteria:**\n- [ ] Six named patterns exist: `aws_account_in_arn`, `aws_account_labelled`, `kms_key_uuid`, `gcp_service_account_email`, `gcp_project_id`, `cloud_bucket_uri`\n- [ ] Reserved/fake values do not fire: AWS `123456789012`, GCP project `kinoforge-prod-deadbeef`, SA project part `proj`, buckets `bkt`/`bucket`/`my-bucket`/`layer-w-test`/`probe-discard`\n- [ ] `kinoforge: allow-identifier` on a line suppresses every finding on that line\n- [ ] `scan_all_tracked_identifiers` enumerates via `git ls-files -z`, skips binary (NUL in first 8000 bytes), skips `FileNotFoundError`, raises `RuntimeError` on any other read failure\n- [ ] Reverse test: a planted identifier in a throwaway git repo is found end-to-end through `scan_all_tracked_identifiers`\n\n**Verify:** `pixi run python -m pytest tests/tools/test_scan_identifiers.py -v` -> all pass\n\nFull code for both files is in the plan doc, Task 0 Steps 1 and 3. Identifiers in the test file MUST be built by string concatenation (as `tests/test_source_audit.py:98` does) -- a literal would trip the sweep this plan adds.\n\n```json:metadata\n{\"files\": [\"tools/scan_identifiers.py\", \"tests/tools/test_scan_identifiers.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/tools/test_scan_identifiers.py -v\", \"acceptanceCriteria\": [\"six named patterns present\", \"reserved/fake values exempt\", \"allow-identifier pragma suppresses per-line only\", \"tracked-tree sweep fails closed on unreadable files\", \"planted-identifier reverse test passes end-to-end\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 1,
      "nativeTaskId": "2",
      "subject": "Task 1: Repo lockdown test (committed RED)",
      "status": "completed",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** A standing guard over `git ls-files` that fails on HEAD, documenting the ~30 dirty files that Task 2 cleans.\n\n**Files:**\n- Create: `tests/test_cloud_identifier_scrub.py`\n\n**Acceptance Criteria:**\n- [ ] `test_no_tracked_file_contains_a_concrete_identifier` fails on HEAD, listing each `path:line [pattern] value`\n- [ ] Failure message names the fix: use the established fake, a `<PLACEHOLDER>`, or the `kinoforge: allow-identifier` pragma\n- [ ] The RED output is captured into the commit message as the record of what Task 2 must clean\n- [ ] `pixi run pre-commit run --all-files` still passes (pre-commit does not run pytest)\n\n**Verify:** `pixi run python -m pytest tests/test_cloud_identifier_scrub.py -v` -> FAIL, with a finding list\n\nExpected RED shape: `gcp_project_id` 10 files, `cloud_bucket_uri` 20 files, `kms_key_uuid` 3 files, AWS account + SA email 0 files. Full test code is in the plan doc, Task 1 Step 1.\n\n```json:metadata\n{\"files\": [\"tests/test_cloud_identifier_scrub.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cloud_identifier_scrub.py -v\", \"acceptanceCriteria\": [\"lockdown test fails on HEAD with a per-file finding list\", \"failure message names all three remediations\", \"RED output captured in the commit message\", \"pre-commit passes despite the red test\"], \"modelTier\": \"mechanical\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 2,
      "nativeTaskId": "3",
      "subject": "Task 2: Scrub every tracked concrete identifier",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Turn Task 1's lockdown green by replacing every concrete identifier with the established fake or a placeholder.\n\n**Files:**\n- Modify: `.aws/policies/skypilot-minimal.json:163` (KMS UUID -> `<KMS_KEY_ID>`)\n- Modify: `tools/quota_burn_lib.py:266` (project id -> `kinoforge-prod-deadbeef`)\n- Modify: `tests/tools/test_quota_burn_gcp.py`, `tests/tools/test_quota_burn_cli.py`, `tests/tools/test_quota_burn_submit.py`\n- Modify: `PROGRESS.md`, `docs/quota-justification-gcp.md`, `docs/CLOUD-CREDS.md`, `docs/cloud-stores.md`\n- Modify: `docs/superpowers/plans/2026-06-10-gpu-quota-utilization-burn.md` (+ `.tasks.json`), `docs/superpowers/specs/2026-06-10-gpu-quota-utilization-burn-design.md`\n- Modify: `examples/configs/bedrock-nova-reel-t2v.yaml`, `examples/configs/bedrock-luma-ray-t2v.yaml`\n- Modify: `tests/core/test_config.py`, `tests/engines/test_bedrock_video.py`, `tests/engines/test_bedrock_video_replay.py`, `tests/live/test_luma_ray_live.py`, `tests/live/test_nova_reel_live.py`, `tests/live/_c33_probe_h_evidence.json`\n- Modify: `docs/superpowers/research/2026-08-15-cloud-layer-findings-verification.md` (pragma, not scrub)\n- Modify: any further path the Task 1 RED output names\n\n**Acceptance Criteria:**\n- [ ] `test_no_tracked_file_contains_a_concrete_identifier` passes\n- [ ] GCP project ids -> `kinoforge-prod-deadbeef` (convention at `tests/stores/test_recording.py:48-50`)\n- [ ] Bucket names -> `<GCS_BUCKET>` / `<S3_BUCKET>` in docs and example configs; `bkt`-family doubles in tests\n- [ ] KMS UUID -> `<KMS_KEY_ID>` in the policy file\n- [ ] The `kinoforge: allow-identifier` pragma appears ONLY in the F10 section of the verification doc\n- [ ] Full suite still green -- quota-burn tests assert on the project id, so both sides move together\n- [ ] `pixi run kinoforge validate --config examples/configs/bedrock-nova-reel-t2v.yaml` still parses\n\n**Verify:** `pixi run python -m pytest tests/test_cloud_identifier_scrub.py tests/tools/ tests/core/test_config.py tests/engines/ -v` -> all pass\n\nRegenerate the authoritative worklist from the scanner first (plan doc, Task 2 Step 1) -- the file list above is a snapshot.\n\n```json:metadata\n{\"files\": [\"tools/quota_burn_lib.py\", \".aws/policies/skypilot-minimal.json\", \"examples/configs/bedrock-nova-reel-t2v.yaml\", \"examples/configs/bedrock-luma-ray-t2v.yaml\", \"tests/tools/test_quota_burn_gcp.py\", \"tests/core/test_config.py\", \"docs/superpowers/research/2026-08-15-cloud-layer-findings-verification.md\", \"PROGRESS.md\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cloud_identifier_scrub.py tests/tools/ tests/core/test_config.py tests/engines/ -v\", \"acceptanceCriteria\": [\"lockdown test green\", \"project ids use the deadbeef fake including tools/quota_burn_lib.py:266\", \"bucket names placeholdered in docs and example configs\", \"KMS UUID placeholdered\", \"pragma used only in the F10 evidence section\", \"full non-live suite green\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 3,
      "nativeTaskId": "4",
      "subject": "Task 3: Placeholder-render tool for the AWS policy",
      "status": "pending",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** Make `.aws/policies/skypilot-minimal.json` attachable without ever writing a concrete identifier into the tree.\n\n**Files:**\n- Create: `tools/render_aws_policy.py`\n- Test: `tests/tools/test_render_aws_policy.py`\n- Modify: `.aws/policies/skypilot-minimal.json` (`<GCS_KMS_KEYRING>` -> `<S3_BUCKET_PREFIX>`, add `_comment` banner)\n\n**Acceptance Criteria:**\n- [ ] `render()` substitutes `<AWS_ACCOUNT>`, `<KMS_KEY_ID>`, `<S3_BUCKET_PREFIX>`\n- [ ] Raises `ValueError` naming the survivors if any `<...>` placeholder remains after substitution\n- [ ] Raises `ValueError` if the output path resolves inside the repo root\n- [ ] Raises `FileNotFoundError` with a remediation hint when `.aws/kms-test-key.arn` is absent\n- [ ] Rendered output parses as JSON and contains no `<`\n- [ ] `.aws/policies/skypilot-minimal.json` uses `<S3_BUCKET_PREFIX>` in the S3 ARNs and carries the UNVALIDATED banner\n- [ ] The tracked policy file still passes the Task 1 lockdown\n\n**Verify:** `pixi run python -m pytest tests/tools/test_render_aws_policy.py -v` -> all pass\n\nKnown risk carried deliberately: IAM accepts unknown top-level keys today, but that is observed behaviour not a contract. Step 6 checks it parses; Task 9 exercises the real attach. Fallback if rejected: move the banner beside the file, do not drop the UNVALIDATED warning.\n\n```json:metadata\n{\"files\": [\"tools/render_aws_policy.py\", \"tests/tools/test_render_aws_policy.py\", \".aws/policies/skypilot-minimal.json\"], \"verifyCommand\": \"pixi run python -m pytest tests/tools/test_render_aws_policy.py -v\", \"acceptanceCriteria\": [\"all three placeholders substituted\", \"refuses a surviving placeholder naming it\", \"refuses to write inside the repo root\", \"missing ARN file errors with a remediation hint\", \"policy file uses S3_BUCKET_PREFIX and carries the UNVALIDATED banner\", \"policy file still passes the identifier lockdown\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 4,
      "nativeTaskId": "5",
      "subject": "Task 4: GCP minimum roles + securityAdmin revoke",
      "status": "pending",
      "description": "**Goal:** Give GCP the counterpart to the AWS policy file, and turn the `securityAdmin` annotation from a feature into a revoke instruction.\n\n**Files:**\n- Create: `.gcp/policies/roles.txt`\n- Modify: `.gcp/README.md:22-35` (Service account section)\n- Modify: `.gitignore` (unignore `.gcp/policies/`, mirroring `!.aws/policies/` at line 46)\n\n**Acceptance Criteria:**\n- [ ] `.gcp/policies/roles.txt` lists exactly `roles/compute.instanceAdmin.v1`, `roles/iam.serviceAccountUser`, `roles/storage.admin`\n- [ ] The file states the `compute.admin` -> `compute.instanceAdmin.v1` drop and its grounding (`tools/cloud_perms_probe.py:315-318`)\n- [ ] The file states the firewall/VPC caveat and carries the UNVALIDATED banner\n- [ ] The file documents grant-bootstrap-revoke for `roles/iam.securityAdmin` with the exact revoke command\n- [ ] `.gcp/README.md` no longer describes `securityAdmin` as a self-grant convenience\n- [ ] `.gcp/policies/roles.txt` is tracked and passes the Task 1 lockdown\n- [ ] `.gcp/kinoforge-sa.json`, `.gcp/gcloud-config/`, `.gcp/perms-snapshot.json` remain ignored\n\n**Verify:** `git check-ignore -v .gcp/policies/roles.txt` -> exit 1 (not ignored); `git check-ignore -v .gcp/kinoforge-sa.json` -> exit 0 (still ignored)\n\nSecurity-critical: if the SA key comes back NOT ignored after the .gitignore edit, STOP -- the negation is too broad and would track a credential. Full file content is in the plan doc, Task 4 Step 3.\n\n```json:metadata\n{\"files\": [\".gcp/policies/roles.txt\", \".gcp/README.md\", \".gitignore\"], \"verifyCommand\": \"git check-ignore -v .gcp/kinoforge-sa.json && ! git check-ignore -q .gcp/policies/roles.txt && pixi run python -m pytest tests/test_cloud_identifier_scrub.py tests/test_source_audit.py -v\", \"acceptanceCriteria\": [\"roles.txt lists exactly the three runtime roles\", \"instanceAdmin.v1 drop grounded in cloud_perms_probe.py:315-318\", \"firewall/VPC caveat and UNVALIDATED banner present\", \"grant-bootstrap-revoke documented with the revoke command\", \"README no longer frames securityAdmin as a convenience\", \"SA key and snapshot still gitignored\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 5,
      "nativeTaskId": "6",
      "subject": "Task 5: Invert the AWS and GCP onboarding order",
      "status": "pending",
      "blockedBy": [
        3,
        4
      ],
      "description": "**Goal:** The scoped path is what a new operator reads and pastes first; the wide grants stay available, second, labelled with their consequence.\n\n**Files:**\n- Modify: `.env.example:54-94` (GCP and AWS blocks)\n- Modify: `.aws/README.md:19-41` (Bootstrap section) and the \"SkyPilot policy -- apply instructions\" section at line 88\n\n**Acceptance Criteria:**\n- [ ] `.env.example`'s AWS block leads with `render_aws_policy.py` + `put-user-policy`\n- [ ] The FullAccess recipe appears below, labelled a bootstrap shortcut, with its consequence stated\n- [ ] `IAMFullAccess` and `ServiceQuotasFullAccess` are named as what the project actually ran on (per F9), and marked not-recommended\n- [ ] `.env.example`'s GCP block loops over `.gcp/policies/roles.txt` instead of hardcoding `roles/compute.admin`\n- [ ] `.env.example`'s GCP block includes the `securityAdmin` revoke step\n- [ ] `.aws/README.md` bootstrap step 1 no longer says \"attach `AmazonS3FullAccess`\"\n- [ ] `.aws/README.md` puts the CLI render-and-attach path above the console-paste steps, and its step 4 points at the rendered file not the tracked one\n- [ ] `.env.example` still parses through `dotenv_values`\n\n**Verify:** `pixi run python -m pytest tests/test_cloud_identifier_scrub.py tests/test_source_audit.py -v` then `pixi run python -c \"from dotenv import dotenv_values; assert dotenv_values('.env.example')\"` -> PASS\n\nExact replacement text for every block is in the plan doc, Task 5 Steps 1-3.\n\n```json:metadata\n{\"files\": [\".env.example\", \".aws/README.md\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cloud_identifier_scrub.py tests/test_source_audit.py -v\", \"acceptanceCriteria\": [\"AWS block leads with render + put-user-policy\", \"FullAccess relabelled as a bootstrap shortcut with its consequence stated\", \"IAMFullAccess/ServiceQuotasFullAccess documented as what actually ran, not recommended\", \"GCP block loops over roles.txt\", \"GCP block includes the securityAdmin revoke\", \".aws/README.md step 1 drops the FullAccess default\", \".env.example still parses\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 6,
      "nativeTaskId": "7",
      "subject": "Task 6: Billing-alert onboarding step",
      "status": "pending",
      "blockedBy": [
        5
      ],
      "description": "**Goal:** Put a spending ceiling in the onboarding path, because nothing in this repo can enforce one.\n\n**Files:**\n- Modify: `.env.example` -- new section after the compute-provider block (after Modal), before \"Artifact storage\"\n\n**Acceptance Criteria:**\n- [ ] The section sits between credential creation and artifact storage, not appended at the end\n- [ ] AWS Budgets and GCP Budgets-and-alerts both have concrete console paths\n- [ ] Per-provider caps for RunPod, Modal and Replicate are named\n- [ ] The section states why a console budget is the only real ceiling, citing that `est_spend` is wall-clock, that the sweeper reaps only what the ledger knows, and that a scoped credential bounds what launches not how much\n- [ ] `GCP_BILLING_ACCOUNT_ID` is cross-referenced, not duplicated\n- [ ] `.env.example` still parses and the key count is unchanged (the section is comments only)\n\n**Verify:** `pixi run python -c \"from dotenv import dotenv_values; assert dotenv_values('.env.example')\"` -> no output, exit 0\n\nExact section text is in the plan doc, Task 6 Step 1.\n\n```json:metadata\n{\"files\": [\".env.example\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cloud_identifier_scrub.py -v\", \"acceptanceCriteria\": [\"section placed between credential creation and artifact storage\", \"AWS and GCP console paths concrete\", \"RunPod/Modal/Replicate caps named\", \"states why the console is the only real ceiling\", \"GCP_BILLING_ACCOUNT_ID cross-referenced not duplicated\", \"key count unchanged\"], \"modelTier\": \"mechanical\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 7,
      "nativeTaskId": "8",
      "subject": "Task 7: .env.example drift test",
      "status": "pending",
      "blockedBy": [
        6
      ],
      "description": "**Goal:** Catch documentation drift in both directions -- keys documented but consumed by nothing, and operator-facing vars consumed but documented nowhere.\n\n**Files:**\n- Create: `tests/test_env_example_drift.py`\n- Modify: `.env.example` (add `# UNIMPLEMENTED` markers to 11 keys; add `KINOFORGE_S3_BUCKET`)\n\n**Acceptance Criteria:**\n- [ ] `.env.example` parses through `dotenv_values` -- the same parser `kinoforge.core.dotenv_loader:57` uses\n- [ ] `RUNPOD_TERMINATE_KEY=${RUNPOD_API_KEY}` is asserted to expand, not just to be present\n- [ ] Every `KEY=` is referenced in tracked non-doc source, or carries `# UNIMPLEMENTED` / `# OPTIONAL` on the line directly above\n- [ ] The 11 currently-unconsumed keys (Azure + R2 + B2 store vars) get `# UNIMPLEMENTED` markers\n- [ ] `KINOFORGE_S3_BUCKET` is added to `.env.example` -- read at `src/kinoforge/stores/s3/__init__.py:239`, documented nowhere\n- [ ] The reverse check runs against a small hand-curated `_OPERATOR_FACING_VARS`, not all `KINOFORGE_*`\n- [ ] A guard test asserts `_OPERATOR_FACING_VARS` holds no var the codebase has stopped reading\n\n**Verify:** `pixi run python -m pytest tests/test_env_example_drift.py -v` -> all pass\n\nExpected RED before the fix: 11 unmarked keys forward, `KINOFORGE_S3_BUCKET` reverse. Do NOT add `KINOFORGE_S3_PREFIX` on the assumption a sibling exists -- check `rg -n 'KINOFORGE_S3_PREFIX' src/` first. Full test code is in the plan doc, Task 7 Step 1.\n\n```json:metadata\n{\"files\": [\"tests/test_env_example_drift.py\", \".env.example\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_env_example_drift.py -v && pixi run test\", \"acceptanceCriteria\": [\"parses through dotenv_values\", \"RUNPOD_TERMINATE_KEY expansion asserted not just presence\", \"every key consumed or marked on the line above\", \"11 unconsumed keys marked UNIMPLEMENTED\", \"KINOFORGE_S3_BUCKET documented\", \"reverse check uses a curated list\", \"curated list guarded against its own drift\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 8,
      "nativeTaskId": "9",
      "subject": "Task 8: Scoped-policy validation tool",
      "status": "pending",
      "blockedBy": [
        3,
        4
      ],
      "description": "**Goal:** A tool that exercises both scoped grants through IAM simulation, with injected clients so its own tests need no SDK and no network.\n\n**Files:**\n- Create: `tools/validate_scoped_policy.py`\n- Test: `tests/tools/test_validate_scoped_policy.py`\n\n**Acceptance Criteria:**\n- [ ] `validate_aws` runs the two-pass simulation -- wildcard actions against `*`, KMS actions against the key ARN -- mirroring `tools/cloud_perms_probe.py:217-253`\n- [ ] Every action in `_REQUIRED_AWS_ACTIONS` is simulated; any non-`allowed` decision is reported by name\n- [ ] The throwaway IAM user is deleted even when simulation raises\n- [ ] `validate_gcp` calls `testIamPermissions` and reports each missing permission by name\n- [ ] Both take injected clients; the tests import no boto3 and no google SDK\n- [ ] Exit code 0 only when nothing is denied or missing\n\n**Verify:** `pixi run python -m pytest tests/tools/test_validate_scoped_policy.py -v` -> all pass\n\nThe two-pass KMS split is required, not stylistic: the IAM simulator rejects a mixed list of `*` and specific ARNs, which `tools/cloud_perms_probe.py:201-241` already documents. Full code is in the plan doc, Task 8 Steps 1 and 3.\n\n```json:metadata\n{\"files\": [\"tools/validate_scoped_policy.py\", \"tests/tools/test_validate_scoped_policy.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/tools/test_validate_scoped_policy.py -v\", \"acceptanceCriteria\": [\"two-pass simulation with KMS scoped to the key ARN\", \"all 15 required actions simulated\", \"denied actions reported by name\", \"throwaway user deleted on the raising path too\", \"validate_gcp reports missing permissions by name\", \"tests import no cloud SDK\"], \"modelTier\": \"standard\", \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    },
    {
      "id": 9,
      "nativeTaskId": "10",
      "subject": "Task 9: Run validation, record the real result",
      "status": "pending",
      "blockedBy": [
        7,
        8
      ],
      "description": "**Goal:** Actually exercise both scoped grants against the real IAM and Resource Manager APIs, and write down what came back.\n\n**USER-ORDERED GATE -- NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Modify: `.aws/policies/skypilot-minimal.json` (banner reflects the actual result)\n- Modify: `.gcp/policies/roles.txt` (same)\n- Modify: `PROGRESS.md` (RESUME SNAPSHOT entry)\n\n**Acceptance Criteria:**\n- [ ] `pixi run python tools/validate_scoped_policy.py --cloud aws --policy-file <rendered>` was run against real AWS and its JSON output captured verbatim\n- [ ] `pixi run python tools/validate_scoped_policy.py --cloud gcp --project <project>` was run against real GCP and its JSON output captured verbatim\n- [ ] Every action under `denied` (AWS) and every permission under `missing` (GCP) is either fixed in the policy/role list or written down as a known gap with the reason\n- [ ] The throwaway IAM user does not exist afterwards: `aws iam get-user --user-name kinoforge-scope-probe` returns `NoSuchEntity`\n- [ ] The rendered policy file under `/tmp` is deleted\n- [ ] Both banners state the real outcome -- \"simulate-clean, launch-unvalidated\" or \"simulate-denied on N actions\" -- not an aspiration\n- [ ] Zero compute spend: no EC2 instance and no GCE instance was created at any point\n\n**Verify:**\n```\npixi run python tools/validate_scoped_policy.py --cloud aws \\\n  --policy-file /tmp/skypilot-minimal.rendered.json ; echo \"aws rc=$?\"\npixi run python tools/validate_scoped_policy.py --cloud gcp \\\n  --project \"$(pixi run -e live-skypilot gcloud config get-value project)\" ; echo \"gcp rc=$?\"\naws iam get-user --user-name kinoforge-scope-probe 2>&1 | tail -2\n```\nExpected: two JSON reports with `denied: []` / `missing: []` (rc=0) or an explicit list, and `NoSuchEntity` for the probe user.\n\nTwo constraints that must not be traded away. (1) Do NOT widen a `Resource` to `\"*\"` to clear a denial -- a scoped policy quietly widened to pass its own test is worse than an honest failing one; record the gap in the banner instead. (2) The GCP run measures permission NAMES, not the narrow role set, unless `roles.txt` is bound to a second SA and the run happens as that identity -- if that is not possible this session, record the weaker claim rather than overstating it. Identity probes only, never inspect a key value. If AWS credentials are dead (plausible: Phase 53 abandoned this surface 2026-06-17), record \"not run -- no working credential at HEAD\" and proceed to the GCP half rather than minting a new key.\n\n```json:metadata\n{\"files\": [\".aws/policies/skypilot-minimal.json\", \".gcp/policies/roles.txt\", \"PROGRESS.md\"], \"verifyCommand\": \"pixi run python tools/validate_scoped_policy.py --cloud aws --policy-file /tmp/skypilot-minimal.rendered.json; aws iam get-user --user-name kinoforge-scope-probe 2>&1 | tail -2\", \"acceptanceCriteria\": [\"AWS SimulatePrincipalPolicy run against real IAM, JSON captured verbatim\", \"GCP testIamPermissions run against real Resource Manager, JSON captured verbatim\", \"every denied action and missing permission fixed or recorded as a known gap with reason\", \"throwaway IAM user returns NoSuchEntity afterwards\", \"rendered /tmp policy deleted\", \"both banners state the measured outcome not an aspiration\", \"no EC2 or GCE instance created at any point\"], \"modelTier\": \"frontier\", \"userGate\": true, \"tags\": [\"user-gate\"], \"gateScope\": \"task\", \"failurePolicy\": \"halt\", \"requireEvidenceTokens\": [[\"aws\", \"SimulatePrincipalPolicy\", \"denied\"], [\"gcp\", \"testIamPermissions\", \"missing\"]], \"planPath\": \"docs/superpowers/plans/2026-08-21-least-privilege-onboarding.md\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-23T00:41:58.123504-07:00"
}
