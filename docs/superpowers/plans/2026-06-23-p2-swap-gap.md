# P2 swap-gap plan — 2026-06-23

Spec: `docs/superpowers/specs/2026-06-23-p2-swap-gap-design.md`.

Closes the 2 server-side gaps in
`src/kinoforge/engines/diffusers/servers/wan_t2v_server.py`
`/lora/set_stack` exposed by Tier-4 case_5 + case_7 on 2026-06-23.

Branch: `main` (continuation of the in-flight P2 workstream — Tier-4
PARTIAL_GREEN sits on `main` at HEAD `63473a7`).

Stub-first TDD per user choice (2026-06-23): RED Tier-1 unit tests
against the existing `KINOFORGE_STUB_MOE=1` stub → patch → green →
ONE live Tier-4 re-fire at $4 cap.

## Tasks

- [ ] **T1 — Design + plan docs** ← THIS COMMIT
  - `docs/superpowers/specs/2026-06-23-p2-swap-gap-design.md`
  - `docs/superpowers/plans/2026-06-23-p2-swap-gap.md`

- [ ] **T2 — Stub minor extension** (RED-supporting)
  - `tests/smoke/local_cpu/stub_pipe.py`: extend
    `_TransformerRecorder` and `_FaithfulStubPipe.load_lora_weights`
    to record per-load `(adapter_name, transformer_attr)` tuples so
    the new tests can assert routing without spinning a real pipe.
  - No behavior change for existing local-CPU smoke; additive
    instrumentation only.

- [ ] **T3 — RED Tier-1 unit tests**
  - `tests/engines/diffusers/servers/test_set_stack_swap_gaps.py`:
    - `test_same_ref_branch_swap_preserves_file_on_disk` (T-A invariant
      from spec §5).
    - `test_same_ref_two_branches_yields_two_inventory_rows` (T-B).
    - `test_branch_swap_does_not_double_count_freed_disk` (T-C).
  - All 3 RED against unmodified server. Verified by running pytest
    pre-fix; expected `KeyError` / file-missing failure mode.
  - Tests speak through the live FastAPI `TestClient` against the
    `wan_t2v_server.app` instance with stub installed via
    `KINOFORGE_DIFFUSERS_LOAD_STUB` + `KINOFORGE_STUB_MOE=1`, mirroring
    the existing `tests/engines/diffusers/servers/test_wan_t2v_server.py`
    harness shape.

- [ ] **T4 — Patch `_evict_one`** (`wan_t2v_server.py:574-602`)
  - Pop inventory first; check surviving `(ref, *)` entries; only
    unlink the file when no sibling remains. Composite-identity Q6
    contract.
  - T-A goes from RED to partial-GREEN (file survives) but inventory
    coverage still missing — T-A asserts both file + inventory state,
    so still RED after T4 alone.

- [ ] **T5 — Patch `/lora/set_stack` handler** (`wan_t2v_server.py:967-1222`)
  - Insert target-key seeding pass BEFORE the eviction step (spec §3.3).
  - Recompute `current_keys` + `mandatory_evict` AFTER the seeding pass.
  - Recompute `mandatory_freed` accounting (spec §3.4) so a
    branch-swap eviction doesn't pretend to free its file's bytes.
  - T-A, T-B, T-C all go GREEN.

- [ ] **T6 — Full suite verification**
  - `pixi run pytest tests/engines/diffusers tests/core tests/_smoke_harness`
    GREEN. Zero regressions vs the 2984/83/6 baseline.
  - `pixi run pre-commit run --all-files` GREEN before commit.

- [ ] **T7 — Atomic commits + PROGRESS.md update (code-only state)**
  - One commit per logical step (design+plan / stub / RED tests /
    `_evict_one` fix / handler fix / PROGRESS update).
  - PROGRESS.md flips Tier-4 from PARTIAL_GREEN to
    `CODE-COMPLETE — awaiting live re-fire`.
  - Live re-fire stays the single next action.

- [ ] **T8 — Live Tier-4 re-fire**
  - `pixi run preflight` — pass.
  - `KINOFORGE_LIVE_TESTS=1 pixi run pytest tests/smoke/release_wan22/test_dual_transformer_routing.py -x`
    with `--no-reuse` injection via the existing release-gate
    harness's pod-lifecycle path.
  - Poll RunPod every 60-90s per CLAUDE.md monitoring rule.
  - Cap: $4 — kill the pod if `costPerHr * elapsed_h > $4`.
  - Post-run `pixi run kinoforge list` — verify zero pods.

- [ ] **T9 — successful-generations.md §11 + PROGRESS.md FULL_GREEN**
  - Append per-case sha256 for cases 5 + 7.
  - PROGRESS.md flips Tier-4 to 7/7 FULL_GREEN.
  - Commit + push.

## Verification chain

| Task | RED → GREEN evidence |
|---|---|
| T3 | `pytest tests/engines/diffusers/servers/test_set_stack_swap_gaps.py` 0/3 PASS pre-T4. |
| T4 | After patch, T-A/T-B/T-C still RED (T-A passes file-survives sub-assertion, fails inventory; T-B unchanged; T-C still wrong). |
| T5 | After patch, 3/3 PASS. |
| T6 | `pixi run pytest tests/engines tests/core tests/_smoke_harness`: zero regressions. |
| T8 | Tier-4 7/7 PASS on a single re-fire. Cumulative spend under $4. |
