"""SeedVR2 upscaler adapter (ByteDance-Seed/SeedVR).

The public :class:`SeedVR2Engine` is added by Task 10; until then this
package exists as a stable import path for the runtime + weights helpers.
"""
